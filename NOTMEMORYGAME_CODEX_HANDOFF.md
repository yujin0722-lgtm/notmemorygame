# NOT MEMORY GAME — Codexへの開発申し送り

作成日：2026-07-27  
暫定プロジェクト名：**notmemorygame**  
GitHubリポジトリ：`https://github.com/yujin0722-lgtm/notmemorygame`

---

## 1. この文書の目的

ChatGPTとの対話で設計・試作したブラウザゲームを、今後はCodexがGitHubリポジトリ上で継続開発するための申し送りである。

現在、遊べるプロトタイプv2は完成しているが、GitHubリポジトリにはまだゲーム本体が登録されていない。リポジトリの`main`ブランチには、ユーザーが作成した簡易的な`README.md`だけが存在する。

Codexが最初に行う作業は、**添付された最新版ソースをリポジトリへ登録し、GitHub Pagesで動作確認できる状態にすること**である。

---

## 2. ユーザーと作業するときの重要方針

ユーザーは非エンジニアであり、自分でコードを書くことは基本的に想定していない。

以下を守ること。

- コードの断片だけを渡さず、Codex側で対象ファイルを直接編集する。
- 変更後は、何を変更したか、どこを確認すべきかを日本語で簡潔に説明する。
- 一度に大規模な再設計を行わず、小さな変更単位で遊べる状態を維持する。
- 現在の動作を壊さないことを優先し、変更前後の確認を行う。
- ユーザーから既に得ている仕様を、同じ質問で聞き直さない。
- 必要のないフレームワーク導入や過剰な機能追加を避ける。
- ゲームデザイン上の提案と、実装済みの仕様を混同しない。
- ユーザーが動作確認できるURLまたは完成ファイルを必ず示す。

現在はプロトタイプ段階なので、構造を必要以上に複雑にしないこと。静的なHTML・CSS・JavaScriptのまま開発を続ける方針でよい。

---

## 3. ゲームのコンセプト

神経衰弱をモチーフにした、ローグライト型の1画面ブラウザゲーム。

通常の神経衰弱と異なり、カードは規則正しいグリッドではなく、画面内にバラバラに散らばって配置される。プレイヤーは1ターンに2種類の「フリップ」を順番に使い、公開済みカードを含めてペアを作る。

ラウンドごとにカードが1枚増えるため難易度は徐々に上がるが、プレイヤーも特殊フリップを獲得し、配置に応じて装備を変えながら攻略する。

重要な面白さは次の3点。

1. **カード配置を見てから、使用する特殊フリップを選ぶこと**
2. **フリップ1の結果を見て、フリップ2の対象を判断すること**
3. **失敗するとライフを失うが、公開されたカードは次ターンの情報として残ること**

---

## 4. 確定しているゲーム進行

### ラウンド開始

1. そのラウンドで使うカードを生成する。
2. すべて裏向きで、画面内にランダムに散らして配置する。
3. プレイヤーはカード配置を見たうえで、フリップ1とフリップ2の装備を選ぶ。
4. 「この装備で開始」を押してターンを開始する。

ラウンド開始後にカード位置は変更しない。

### ターン開始

装備したフリップ1、フリップ2を順番に使用する。

### アクションフェイズ

1. フリップ1を実行する。
2. 公開結果を確認できる。
3. フリップ2を実行する。

フリップ1と2は、原則として別々に対象カードを選択する。

### 判定フェイズ

フリップ1・2で新しく表になったカードだけでなく、**前ターンから表向きで残っている未処理カードも含めて**ペアを判定する。

- 1組以上のペアがある：ターン勝利
- 複数ペアがある：コンボボーナス
- 成立したペア：盤面から消える
- ペアにならなかった表向きカード：そのまま残る
- ペアが1組もない：ターン敗北、ライフを1失う

### ゲームオーバー判定

ターン敗北によりライフが0になった場合、その時点で即ゲームオーバーとする。

裏向きカードが1枚以下になっていても、ライフ0なら次ラウンドには進まない。

### エンドフェイズ

プレイヤーが生存している場合のみ実行する。

- 裏向きカードが2枚以上：次のターンへ
- 裏向きカードが1枚以下：ラウンド終了

ラウンド終了後、カードを1枚増やして次ラウンドへ進む。次ラウンドでは新しいデッキを生成し、再配置する。

---

## 5. カード構成

ラウンド番号を`R`とすると、カード枚数は`R + 1`枚。

例：

| ラウンド | カード構成例 |
|---:|---|
| 1 | A・A |
| 2 | A・A・B |
| 3 | A・A・B・B |
| 4 | A・A・B・B・C |
| 5 | A・A・B・B・C・C |

奇数枚の場合、最後の絵柄は1枚だけであり、そのカードにはそのラウンド中の相方が存在しない。

カード表面は現在、絵柄を識別しやすい大きな絵文字で表示している。

---

## 6. フリップ仕様

1ターンには、装備したフリップ1とフリップ2を各1回ずつ使用する。

### 通常フリップ

- 選択した裏向きカードを1枚公開する。
- 特殊フリップの所持数には含めない。
- 両方の枠に同時装備可能。

### ダブル

- 選択したカードを公開する。
- さらに、選択カードから中心点の距離が最も近い裏向きカードを1枚公開する。
- 合計最大2枚。
- 配置を読み、狙って使う精密型。

### ラッキーストライク

- 選択したカードを公開する。
- さらに、ほかの裏向きカードからランダムに2枚を公開する。
- 合計最大3枚。
- カードが不足する場合は、残っているカードだけを公開する。
- 散らばった盤面で有利なギャンブル・探索型。

### パワーアタック

- 選択したカードを公開する。
- さらに、そのカードを中心とした一定半径内の裏向きカードをすべて公開する。
- 密集した配置で有利。
- 現在の半径はカード幅の`2.25倍`。
- マウスオーバー時に範囲をプレビューする。

### 将来用のコスト

各フリップ定義には`lifeCost`を持たせており、強力なフリップにライフ消費などの代償を設定できる構造になっている。現在はすべて0。

---

## 7. 特殊フリップの所持と装備

- 特殊フリップ所持上限：**4種類**
- 同時装備枠：**2枠**
- 通常フリップは所持上限に含めない
- 同じ特殊フリップは重複所持しない
- 同じ特殊フリップを両方の装備枠に設定できない
- 装備変更は各ラウンドのカード配置後、開始前のみ
- 現在の特殊フリップは3種類だけなので、所持上限4の効果は将来フリップ追加後に現れる

2ラウンドをクリアするごとに、未所持の特殊フリップが報酬候補として表示される。所持上限に達した場合は、所持中のフリップと入れ替えるか、新しいフリップを諦める。

将来的には以下の入手方法も想定しているが、まだ未実装。

- ラウンドクリア報酬
- 特殊条件達成によるアンロック
- ランダム出現するショップで購入
- レアリティ
- 強力な効果に対するライフ・スコア・クールダウンなどの代償

---

## 8. 現在のスコアとライフ

- 初期ライフ：5
- ペア得点：`100 × 同一ターンのペア数²`
  - 1ペア：100点
  - 2ペア：400点
  - 3ペア：900点
- ラウンド突破ボーナス：`ラウンド番号 × 50点`
- ペアなし：ライフを1失う
- ライフ0：即ゲームオーバー

これらは暫定値であり、今後プレイテストにより調整する。

---

## 9. 現在の実装ファイル

最新版ソースには以下が含まれる。

```text
index.html   画面構造
styles.css   UI、カード、モーダル、アニメーション
config.js    調整値とフリップ定義
game.js      状態管理、配置、ターン、判定、報酬、装備処理
README.md    起動方法と簡易仕様
preview.png  画面参考画像
```

外部ライブラリ、npm、ビルドツール、バックエンドは使用していない。`index.html`をブラウザで直接開いても動作する。

### 設計方針

- 調整値は可能な限り`config.js`へ集約する。
- DOM構造は`index.html`、外観は`styles.css`、ゲームロジックは`game.js`に分離する。
- まずは単純な静的Webゲームとして維持する。
- フレームワークへの移行は、明確な必要性が生じるまで行わない。

---

## 10. 現在のプロトタイプで確認済みの内容

- Chromeでローカル起動できる
- カードが重ならないよう散らばって配置される
- カード配置後に装備選択パネルが表示される
- 表向きカードの絵柄が確認できる
- 通常、ダブル、ラッキーストライク、パワーアタックが動作する
- 前ターンの表向きカードを含めてペア判定される
- ペア成立時にカードが消える
- ペアなしでライフが減る
- ライフ0でゲームオーバーになる
- 次ラウンドでカードが1枚増える
- 報酬、所持、装備変更の基本処理が動作する
- JavaScriptの構文チェックは通過している

ただし、網羅的な自動テストや、多数のブラウザ・スマートフォンでの検証はまだ行っていない。

---

## 11. 現在のリポジトリ状態

リポジトリ：`yujin0722-lgtm/notmemorygame`

2026-07-27時点では、`main`ブランチに簡易READMEのみが存在する。ゲーム本体はまだコミットされていない。

通常のChatGPT GitHub連携はリポジトリの読み取りはできたが、書き込み時に403となったため、今後の直接編集はCodexへ移行する。

GitHub Pagesの設定は未確認。Codexは初期登録後に、以下を確認すること。

- 公開元：`main`ブランチ
- 公開フォルダ：リポジトリルート `/`
- 想定URL：`https://yujin0722-lgtm.github.io/notmemorygame/`

リポジトリ設定をCodexから変更できない場合は、ユーザーへGitHub画面上の最短手順を案内する。

---

## 12. Codexが最初に行う作業

1. `yujin0722-lgtm/notmemorygame`を取得する。
2. ユーザーから渡された最新版ソース一式を確認する。
3. 現在の簡易READMEを、最新版ソース内のREADMEへ置き換える。
4. `index.html`、`styles.css`、`config.js`、`game.js`をリポジトリルートへ追加する。
5. ローカルまたはプレビュー環境で起動する。
6. 少なくとも以下を手動確認する。
   - ラウンド1を開始できる
   - カード表面が見える
   - フリップ1→フリップ2→判定の順に進む
   - ラウンド2で配置を見てから装備変更できる
   - ライフ0で即ゲームオーバーになる
7. 初期登録を1つの明確なコミットとして`main`へ反映する。
8. GitHub Pagesを有効化し、公開URLで表示を確認する。
9. ユーザーへ公開URLと確認項目を日本語で伝える。

推奨コミットメッセージ：

```text
Import playable prototype v0.2
```

---

## 13. 今後のGit運用

プロトタイプ調整中は、ユーザーの操作負担を増やさないことを優先する。

推奨：

- `main`は常に遊べる状態を維持する。
- 小さな修正は、1テーマにつき1コミットで`main`へ反映してよい。
- 大きなルール変更や構造変更の前には、タグまたは作業ブランチを作る。
- 安定した節目には`v0.x`形式のタグを付ける。
- 公開後に利用者が増えた段階で、PRベースの運用へ切り替える。

ユーザーにGitコマンドを入力させることを前提としない。

---

## 14. 既知の注意点・今後確認すべき点

### 配置アルゴリズム

カード同士が重ならないランダム配置を試し、難しい場合は少し崩した格子配置へフォールバックする。高ラウンドでは「バラバラに散っている」という意図が弱くなる可能性があるため、カード枚数増加時の見た目を確認すること。

### 画面サイズ変更

ラウンド準備中などは再配置するが、アクション中・判定中には位置を変えない。レスポンシブ表示とスマートフォン操作は今後要検証。

### 早期判定

現実装では、フリップ1だけですべての裏向きカードが公開された場合、フリップ2を行わず判定へ進む。これは合理的な暫定仕様だが、正式ルールとして維持するかは今後確認する。

### 報酬候補

未所持フリップだけを候補にする。現在は特殊フリップが3種類しかないため、すべて獲得すると以後報酬は表示されない。

### バランス

現在のライフ、得点、報酬頻度、パワーアタック半径は暫定。ユーザーのプレイ感を優先し、数値調整は`config.js`で行う。

---

## 15. まだ実装しない方がよいもの

ゲームの基本ループが十分に検証されるまでは、以下を一度に追加しない。

- 大規模なショップシステム
- 多数のレアリティと複雑な確率テーブル
- アカウントやオンラインランキング
- バックエンド
- 大規模なセーブデータ設計
- 不要なフレームワーク移行
- 過剰な演出や素材制作

まずは、配置を見てフリップを選ぶ判断と、2回のフリップを組み合わせるターンが本当に面白いかを検証する。

---

## 16. Codexへの最初の依頼文（そのまま使用可能）

```text
このリポジトリの継続開発をお願いします。
添付したNOTMEMORYGAME_CODEX_HANDOFF.mdを最初に読み、同時に添付した最新版ソースを正本として扱ってください。

最初の作業は、現在READMEだけの yujin0722-lgtm/notmemorygame にゲーム本体を登録し、Chromeで基本動作を確認したうえで、GitHub Pagesから遊べる状態にすることです。

大規模な書き直しはせず、現在のHTML・CSS・JavaScript構成を維持してください。ユーザーは非エンジニアなので、コード断片を提示するのではなく、必要なファイルを直接編集・コミットしてください。完了後は、変更内容、確認した動作、GitHub PagesのURLを日本語で報告してください。
```

---

## 17. 引き継ぎ時にCodexへ渡すもの

必須：

1. このファイル：`NOTMEMORYGAME_CODEX_HANDOFF.md`
2. 最新版ソース一式：`notmemorygame-codex-source-v0.2.zip`
3. GitHubリポジトリ：`https://github.com/yujin0722-lgtm/notmemorygame`

以上。
