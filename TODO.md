# NOT MEMORY GAME — TODOリスト

ユーザーから出されたアイデアを記録し、今後、仕様を詰めたうえで実装する改善点を管理する文書です。

## 運用ルール

- この文書の項目は、明示的な実装依頼があるまで**未実装のまま保持**します。
- ユーザーが「TODOリストに追加」と指定した内容は、追加時点ではコーディングしません。
- 実装前に「要検討事項」を確認し、確定した仕様をこの文書または関連仕様書へ反映します。
- 実装が完了し動作確認できた項目だけ、ステータスを「完了」に変更します。

## 実装済みの大きな変更

### トランプ化（2026-07-29 完了）

- **内容：** カードを架空シンボル（絵文字）から実際のトランプ52枚に変更。ラウンド1は「同じ色・同じ数字」のペア（例：スペードの3とクラブの3）がランダムに選ばれて開始し、以降は毎ラウンド新しいカードを1枚ずつ追加（`config.js`の`newCardsPerRound`で調整可）。ペアは「同じ数字・同じ色（黒:♠♣ / 赤:♥♦）」で成立し、未成立カードは相方が出るまでラウンドをまたいで盤面に残り続ける。ラウンド内で成立したペアは除去されるが、新しいラウンドでは残っているカードも含めて全て裏向きのままランダムに再配置される。52枚をすべて使い切るとゲームクリア（全51ラウンド）。
- **変更ファイル：** `config.js`（`symbols`廃止・`newCardsPerRound`追加）、`game.js`（デッキ生成・ラウンド進行・盤面持ち越し・クリア判定）、`index.html`（ゲームクリア用モーダル追加、ルール説明更新）、`styles.css`（トランプ柄の見た目・色分け）、`README.md`（ルール説明更新）。
- **動作確認：** Playwrightでラウンド1〜3の自動プレイ、8回のリセットでラウンド1の開始ペアがランダムに変わることを確認、51ラウンドの完走（ゲームクリア画面表示）を確認済み。コンソールエラーなし。
- **修正履歴：** 初回実装時、ラウンド1の開始ペアが常に「スペードのA・クラブのA」に固定されるバグがあり、ユーザー指摘により修正（2026-07-30）。

## TODO

### アイデア一覧

- 特殊フリップの回数制限実装（3回）
- 特殊フリップ追加：オセロフリップ。2フリップにしか装備できない。1フリップ目に指定したカードとの間にあるカードをめくる
- スコアが1000を超えるごとにライフを1追加する

### 特殊フリップの新規アイデア（序盤：相加的 / 中盤以降：相乗的）

- **ステータス：** 未着手・仕様検討待ち
- **前提：** 特殊フリップ周りの仕様は未確定。ローグライトとして、序盤は効果量が一定で価値計算しやすい「相加的」効果、中盤以降は盤面状態やラン進行度に応じて跳ね上がる「相乗的」効果を分けて用意する方針の叩き台。

#### 序盤向け（相加的）

1. **トリプル**：エネルギー2。選んだカード＋近い順の裏向きカード2枚（合計3枚）を公開。ダブルの上位互換で常に+2枚固定。
2. **エコーフリップ**：エネルギー1。選んだカードと、盤面中心を挟んで点対称の位置に最も近い裏向きカードを公開。距離ではなく配置の形を読む軸で、常に+1枚固定。
3. **セーフティネット**：エネルギー1。選んだカードを1枚公開。このターンの判定が失敗だった場合、ライフ減少を1回だけ無効化（使い切り）。
4. **リザーブチャージ**：エネルギー0。選んだカードを1枚公開＋このターンのみエネルギー+1（フリップ2用に使える）。
5. **リコール**：エネルギー2。選んだカードを1枚公開。加えて、直前のターンで不成立のまま裏に戻った記号のうち1種類を指定し、そのカードの片方を公開。

#### 中盤以降向け（相乗的）

1. **チェインリアクション**：エネルギー2。選んだカードを公開。さらに、既に表向きで残っている未成立カードと同じ記号のカードが盤面にあれば自動で公開。表向きカードが溜まっているほど強くなる。
2. **ダブルダウン**：エネルギー3。単体では何も公開しない。同ターン中に他のフリップで公開された枚数を後から2倍にする増幅役。単独では機能せず、他の装備との組み合わせが前提。
3. **命の刃**：エネルギー1。公開する追加枚数が「失っているライフの割合」に比例して増える（ピンチほど強い）。※下記「あきらめるな！」と同系統のため、仕様確定時に統合を検討。
4. **エコーチェンバー**：エネルギー2。盤面に3枚以上残っている記号があれば、その記号のカードをすべて公開。序盤の少枚数盤面ではほぼ空振りし、高ラウンドで強くなる。
5. **完全記憶**：エネルギー3。これまでのラン全体で成立させたペアの累計数に応じて、公開範囲（半径）にボーナスが乗る。序盤に取ると弱いが終盤まで持ち越すほど強くなる。

- **要検討事項：**
  - 上記5案と、既存の「とどめの一撃」「先手必勝」「あきらめるな！」（本ファイル末尾）との重複整理（特に「命の刃」）。
  - エネルギーコストと`rarity`の対応関係。
  - 「ダブルダウン」のような他フリップ依存効果を、フリップ1・フリップ2どちらの枠にも装備可能にするか。
  - 「完全記憶」のようなラン累計依存効果を、リスタート時にどうリセットするか。

### ゲーム性レビューからの改善案（優先順）

現行ルールを壊さずに試せるよう、まず小さな設定変更や表示追加で検証し、数値はプレイテストで確定します。特に「大量公開が常に最善」「ライフが増え続けて終わらない」「ランダムだけで勝敗が決まる」状態を避けることを共通方針とします。

1. **ラウンドごとのパー（目標ターン数）と評価を追加する**
   - クリア条件は変えず、「パー以内」「ノーミス」「特殊フリップ未使用」などを任意目標として提示し、達成時はスコアだけを加算する。
   - 生存に必須の報酬にしないことで、初心者を詰ませず、上級者にはリスクを取る理由を作れる。
2. **フリップ前に効果範囲と公開枚数を確定表示する**
   - ダブルやパワーアタックの対象候補をハイライトし、実行前に「合計3枚」のように表示する。ラッキーストライクはランダム対象であることだけを明示する。
   - 位置依存の戦略性を読み取れるようにし、意図しない大量公開による理不尽な消費を防ぐ。
3. **特殊フリップに共通のエネルギー制を導入する**
   - 各ラウンド開始時に少量を支給し、強い効果ほど多く消費する。未使用分の持ち越しには上限を設ける。
   - 使い捨て在庫だけに頼らず「今使うか温存するか」を作りつつ、無限温存からの一括攻略を防ぐ。既存の「3回制限」案とは、まずどちらか一方だけを比較検証する。
4. **連続成功のコンボ倍率と安全な上限を追加する**
   - ペア成立を連続させると倍率が上がり、失敗でリセットする。ただし倍率には上限を設け、複数ペアの二乗得点とは乗算せず加算ボーナスにする。
   - 表向きカードを計画的に残すプレイを評価しながら、パワーアタックで得点が指数的に膨らむのを避ける。
5. **ライフ回復を「しきい値報酬の選択肢」にする**
   - 1,000点ごとの自動回復ではなく、到達時に「ライフ1回復」か「得点ボーナス」かを選ぶ。最大ライフと、1ラウンドの回復回数に上限を設ける。
   - 劣勢時には救済、余裕がある時にはスコア狙いとなり、回復の累積でゲームオーバーが事実上なくなることを防ぐ。
6. **奇数ラウンドの1枚余りを明示的な特殊カードにする**
   - ペアにならないカードを専用の「ジョーカー」として見分けられるようにし、公開できれば小得点または次ラウンドの一時効果を与える。
   - 通常絵柄に見えるのに絶対そろわない誤解をなくす。ジョーカーを残しても現行どおりクリアでき、引けなくても攻略不能にしない。
   - **【2026-07-29 対応済み】** トランプ52枚化により、この課題自体が解消。すべてのカードに実在する相方（同じ数字・同じ色の別スート）が必ず存在し、見た目（ランク・スート）からも判別できるため、絶対にそろわないカードは発生しない。
7. **報酬を三択（新規・強化・回復）にして必ず意味のある選択を出す**
   - 未所持フリップがなくても、既存フリップの一時強化、エネルギー、制限付き回復などを候補にする。候補は同系統だけにならないよう生成する。
   - 全種類所持後に報酬イベントが空になる問題を避け、ビルドの方向性と周回ごとの差を作る。
8. **段階的に追加される盤面ギミックを用意する**
   - 例として、1回だけ対象をずらすカード、隣接カードも同時に見せるカード、公開後に位置を交換するカードを高ラウンドから1種類ずつ加える。
   - 同時出現は最大1種類、発動前にルールを表示し、絵柄の完全な再ランダム化は行わない。記憶を無意味にせず盤面判断を増やす。
9. **一定間隔のボスラウンドでルールを一つだけ変える**
   - 5ラウンドごとなどに「ターン上限」「指定ペアを最後に取る」といった追加目標を設定し、通常より良い報酬を出す。
   - 初見殺しを避けるため開始前に条件を開示し、失敗時は即ゲームオーバーではなく通常のライフ損失で再挑戦または通常進行を選べるようにする。
10. **短いラン形式と難易度カーブを定義する**
    - まず10ラウンドを1ランの区切りとして結果画面を出し、その後は任意でエンドレスへ進める。カード枚数、ギミック数、報酬頻度には段階ごとの上限を設ける。
    - 明確な達成点と再挑戦の動機を作り、カードが増え続けて盤面やプレイ時間が破綻するのを防ぐ。

以下に、各アイデアの検討状況と実装前に決める内容を記録します。

### 特殊フリップの回数制限（3回）

- **ステータス：** 未着手・仕様検討待ち
- **ユーザーアイデア：** 特殊フリップの回数制限実装（3回）
- **要検討事項：**
  - 3回という上限を、特殊フリップの種類ごと・取得アイテムごと・ゲーム全体のどの単位で管理するか。
  - 現在の「1回使うと消費」という仕様を、3回使用後に消費する仕様へ置き換えるか。
  - 残り回数を画面のどこに、どのタイミングで表示するか。
  - 再取得、報酬での入れ替え、ゲーム再開時に残り回数をどう扱うか。

### 特殊フリップ「オセロフリップ」の追加

- **ステータス：** 未着手・仕様検討待ち
- **ユーザーアイデア：** 特殊フリップ追加：オセロフリップ。2フリップにしか装備できない。1フリップ目に指定したカードとの間にあるカードをめくる
- **確定条件：**
  - フリップ1には装備できない。
  - フリップ2にのみ装備できる。
  - フリップ1で指定したカードの位置を効果の基準にする。
- **要検討事項：**
  - 「間にあるカード」を判定する形状（直線、一定幅の帯、長方形など）と判定幅。
  - フリップ2で指定したカード自体と両端のカードを公開対象に含めるか。
  - フリップ1が複数枚を公開する特殊フリップだった場合も、効果で公開されたカードではなく指定した1枚だけを起点とするか。
  - すでに表向きのカードや処理済みカードが範囲内にある場合の扱い。
  - 装備選択画面でフリップ1への装備をどう禁止・案内するか。
  - 効果範囲のプレビューを表示するタイミングと見た目。

### スコア1,000点ごとのライフ追加

- **ステータス：** 未着手・仕様検討待ち
- **ユーザーアイデア：** スコアが1000を超えるごとにライフを1追加する
- **要検討事項：**
  - 判定条件を厳密な「1,000を超えた時点」とするか、「1,000以上に到達した時点」とするか。
  - 2,000点、3,000点以降も同様に、各しきい値につき1回だけ追加するか。
  - 1回の得点で複数のしきい値を超えた場合、超えた数だけライフを追加するか。
  - ライフの最大値を設けるか。
  - ラウンド突破ボーナスによるしきい値到達も対象にするか。
  - ライフ追加をプレイヤーへ知らせる演出やメッセージをどうするか。
